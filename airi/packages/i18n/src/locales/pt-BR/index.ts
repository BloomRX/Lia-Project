// Minimal pt-BR locale (M1 Phase 2).
// Only the Lia Home keys are provided here so the launcher works fully in
// pt-BR already. All other namespaces fall back to `en`. The complete pt-BR
// locale is expanded in M1 Phase 4 (i18n).
import home from './home.yaml'

export default {
  tamagotchi: {
    home,
  },
}
