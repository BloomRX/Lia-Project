// pt-BR locale (M1). Ships the Lia Home and the Electron tray menu in pt-BR;
// other namespaces still fall back to `en`. The complete pt-BR locale is
// expanded in M1 Phase 4 (i18n).
import tamagotchi from './tamagotchi'

export default {
  tamagotchi,
}
